Line does not have
 - line.strokeDasharray(dashArray) where dashArray is a String
 - line.strokeOpacity(alpha) where alpha is a Number between 0 and 1

Should the method be added to Line or to a superclass?

Circle does not have 
- circle.fillOpacity(alpha) where alpha is a Number between 0 and 1

Should the method be added to Circle or to a superclass?